@using Automotive_Project.Extensions
@model List<Product>
@{
    ViewData["Title"] = "Home Page";
    ViewData["HomePage"] = true;
}



@* <div style="height:400px; width:300px; margin-right: 120px;">
    <div class="shadow-lg p-3 mb-5 bg-body-tertiary rounded h-100">
        <div class="tab-1">
            <select class="form-select" name="brand" onchange="this.form.submit()">
                <option value="">Choose Car Parts Model</option>
                <option value="">Audi</option>
                <option value="">BMW</option>
                <option value="">Mercedes-Benz</option>
            </select>
        </div>
        <div class="tab-2">
            <select class="form-select" name="brand" onchange="this.form.submit()">
                <option value="">Choose Car Parts Category</option>
            </select>
        </div>
        <div class="tab-3">
            <select class="form-select" name="brand" onchange="this.form.submit()">
                <option value="">Choose Car Parts Year</option>
            </select>
        </div>
        <div class="tab-4">
            <select class="form-select" name="sort" onchange="this.form.submit()">
                <option value="newest">Order By Newest</option>
            </select>
        </div>
    </div>
</div> *@

<div class="bg-light">
    <div class="container py-4">
        <h2 class="pb-4 text-center">Newest Products</h2>

        <div class="row mb-5 g-3">
            @foreach (var product in Model)
            {
                <div class="col-lg-3 col-md-6">
                    <div class="rounded border shadow p-3 text-center h-100">
                        <img src="/images/@product.ImageFileName" class="img-fluid" alt="..."
                             style="height: 180px; object-fit: contain" />
                        <hr />
                        <h5 class="py-2">@product.Name</h5>
                        <p>
                            Brand: @product.Brand, Category: @product.Category
                        </p>
                        <h4 class="mb-3">@(product.Price)$</h4>
                        <a class="btn btn-primary btn-sm mb-2" asp-controller="Store"
                           asp-action="Details" asp-route-id="@product.Id" role="button">Details</a>
                        <button type="button" class="btn btn-warning btn-sm mb-2" onclick="addToCart(this, @product.Id)">
                            Add to Cart <i class="bi bi-cart4"></i>
                        </button>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
